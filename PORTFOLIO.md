# ポートフォリオ — X 分割くん

## プロジェクト概要

X（旧Twitter）のカルーセル表示時代に流行った「分割投稿」（1枚の写真をスライスして複数枚投稿し、タップで縦に大きな画像として見せる手法）を、専門知識ゼロで3ステップで作れるWebツール。

## 技術アーキテクチャ

```
画像ドロップ
   │
   ▼
createImageBitmap (EXIF回転自動補正)
   │
   ▼
仮想キャンバス比の計算（セル比 × 行列数）
   │
   ▼
カバークロップ（ソース矩形計算 + オフセット補正）
   │
   ▼
セル分割（境界のみ gap/2px ずつ削る＝Xカルーセル隙間補正）
   │
   ▼
Canvas描画 → toBlob (PNG/JPEG/WebP)
   │
   ├─ Xタイムライン風プレビュー（カルーセル DOM）
   └─ ZIP一括DL（自前STORE ZIP実装）
```

### 技術的ポイント

#### 1. ギャップ補正アルゴリズム
Xの複数画像（カルーセル）表示では画像間に数pxの隙間が入るため、単純N等分だと境界が欠けて見える。本ツールは分割時に**境界のみ** gap/2px ずつ事前に削る（外周端は削らない）。これにより隙間が入っても自然に繋がる。gapは0〜40pxで調整可能。

#### 2. 任意レイアウトへの自動カバークロップ
元画像のアスペクト比と「セル比×行列数」から逆算した仮想キャンバス比を比較し、はみ出す方向を中央（+オフセット）でクロップ。どの比率の入力画像でも任意の分割レイアウトに対応する。顔や被写体位置に合わせてスライダーで位置調整可能。

#### 3. 依存ゼロのSTORE ZIP実装
画像ファイルは既に圧縮済みのためZIP再圧縮は不要。Local File Header / Central Directory / EOCD を直接構築するSTORE方式ZIPを自前実装（CRC32テーブル生成含む）。CDN・npm依存ゼロ、完全オフライン動作。

#### 4. 逆順ダウンロード
一般にファイラは「新しい順」に並ぶため、1→2→3→4の順で保存すると添付時に逆順に並んでしまう。逆順DLオプションで保存フォルダ内が自然な順に並び、添付ミスを防止する。

## 制約・仕様

- X投稿は1度に4枚まで（5枚以上のレイアウトも生成可能だが警告表示）
- ブラウザのcreateImageBitmap非対応環境（古いブラウザ）では動作しない
- すべての処理がクライアントサイド。ネットワーク通信は投稿画面を開くリンクのみ

## 使用技術

- HTML5 / CSS3（Open Design x-aiデザインシステム準拠のカスタムトークン）
- Vanilla JavaScript (ES2017+)
- Canvas 2D API / createImageBitmap / Blob / File API
- GitHub Pages
