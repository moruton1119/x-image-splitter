# 開発ログ — X 分割くん

## 2026-08-20 v1.0.0

### 調査段階の知見（実装に直結）

流行りの「分割投稿」について調査した際の重要ノウハウ:

1. **単純N等分だとズレる**
   Xの複数画像表示（カルーセル）では画像間に数pxの隙間が入る。単純に4等分して投稿すると境界部分が欠けて見えたり重複して見えたりする。
   → 解決: 分割時に境界を gap/2px ずつ事前に削る（デフォルト12px、調整可能）。先人ツールの知見（Qiita）を踏襲。

2. **ダウンロード順序問題**
   ファイラは大体「新しい順」に並ぶ。1→4の順で保存すると逆順に表示され、X添付時に順番を間違えやすい。
   → 解決: 逆順DLオプション（4→3→2→1の順で保存 → フォルダでは1→2→3→4に並ぶ）。

3. **ブラウザからの自動添付は不可能**
   Web Share APIは複数画像の順序付き添付に非対応。Twitter Web Intentにも画像添付機能はない。
   → 解決: 投稿画面をワンクリックで開くボタン + 添付順序の明示で運用カバー。

### 実装メモ

- **EXIF回転**: `createImageBitmap(blob, {imageOrientation: 'from-image'})` で自動補正。iOSの写真などスマホ縦写真で必須。
- **カバークロップの数学**: 仮想キャンバス比 = セル比(w/h) × cols/rows。入力画像比と比較してはみ出す軸をクロップ。オフセットは「はみ出し最大幅 × (0.5 + offset/100)」で計算。
- **ギャップの安全上限**: gapがセルサイズ/2を超えると破綻するため `min(gap, min(cellW,cellH)/2 - 1)` でクランプ。
- **ZIP**: 画像は圧縮済みなのでSTORE（無圧縮）方式。CRC32テーブルをUint32Array(256)で事前計算。ファイル名はTextEncoderでUTF-8、フラグbit 11 (0x0800) を立てて日本語ファイル名対応。
- **プレビュー**: renderNowをデバウンス120msで呼び、スライダー操作中も軽量。プレビュー描画は360px幅で軽量化、出力はフルサイズで別途生成。
- **JPEG出力時**: 透明背景は黒になるため、先に白fill。

### デザイン

- Open Design（~/open-design Docker）の **x-ai デザインシステム** を採用:
  - #1f2228 ウォームニアブラック + 白テキスト + 不透明度4段階の階調
  - GeistMonoフォールバックスタック（ローカルフォントのみでCDNゼロ）
  - 零シャドウ・ゼログラデーション
- もるとん要件「X風UI・UI自体は変える」に対し、x-aiを土台に**Xブルー #1d9bf0** のCTA（丸ボタン＝ポストボタン風）とXアプリ風ヘッダー（53px）を拡張。

## 2026-08-20 v1.0.1 — プレビュー画像消失バグ修正

### 症状
カルーセルプレビューに番号バッジ（1〜4）だけ表示され、画像が空白。

### 原因
プレビュー用canvas要素を「カルーセル」と「元の並び（flat）」の2箇所に`appendChild`していた。
**`appendChild`は既存DOMを移動させる**ため、flatグリッド構築の瞬間にカルーセルからcanvasが消えていた。番号バッジは別要素のため残存し「バッジだけある空白」に。

### 教訓
- 1つのDOM要素を複数箇所に表示したい時は **blob URL + `<img>`** で独立させる（URLは何度でも参照可能）
- E2Eで「コンテナの表示状態」だけ見ず **`img.naturalWidth > 0`（実画像ロード）を検証**すべきだった
- プレビューに本番blobを使う副次効果: DL内容とプレビューが完全一致

### 追加修正
- 再描画時の旧blob URL解放（`URL.revokeObjectURL`）でメモリリーク防止
- プレビュー幅計算の非表示時フォールバック（clientWidth=0対策）


## 2026-08-20 v2.0.0 — COLLAGE方式への大改修

### もるとんレビューでの指摘（すべて正しかった）
1. **「なんで1080×1080に常に分割してる？分割方法によってアスペクト比は変えなきゃ」**
   → v1はセル比1:1固定のカバークロップで、どんな画像でも正方形セルに無理やり合わせていた。設計思想から間違い。
2. **「プレビューが常に横並びで、分割によってXの表示と同じように変わらないと意味ない」**
   → v1のプレビューは固定高さ360pxの横並びで、縦長セルが「大きく見える」という流行りの本質を再現できていなかった。

### 参考にした実装: collage.mixt-inc.com（VRChatコラージュメーカーの「分割保存」）
- 「写真1枚を左から右へ4枚に切り分け。縦長の4枚を順番に投稿すると、スマホのタイムラインで大きく表示」
- 「キャンバスは写真と同じ比率・余白なしに整えてから切り分け」「元画像の大きさのまま切り分け」
- = **元画像のアスペクト比を維持して切り分けるだけ。クロップしない**

### 流行り「分割投稿」の正体（整理）
- Xのタイムライン画像表示は縦横比の範囲がある（横16:9上限/縦4:5下限が実測ベース）
- 超縦長や大画面の1枚絵をそのまま投稿すると小さく/強クロップされる
- **縦長セルに分割**すれば各枚が高さMAX表示 → 「大きく見える」
- 横長〜正方形画像 → 1×4（左→右）で各セル縦長 / 縦長画像 → 4×1（縦4段）で各セル正方形

### 実装
- calcCells: cropMode='none'（デフォルト）で srcW/H=元画像寸法のまま等分。'4:5'等指定時のみカバークロップ
- xDisplayAR(w,h): Xタイムラインのクロップ仕様シミュレーション（横16:9上限・縦4:5下限、範囲外は「一部クロップ」バッジ）
- プレビュー3種: タイムライン（スマホ幅フレーム+ページングドット+スワイプ連動）/ タップ後（縦連結）/ 元の並び
- autoLayout: AR<0.8→4×1縦4段、それ以外→1×4左→右
- セル情報表示: 「各セル: 512×2048（1:4）— タイムラインで大きく表示されます」をSTEP2/3に表示

### テスト
- E2E v3: 16項目（元比率維持・Xクロップ比4:5/1:1・バッジ・拡大ビュー・cropMode切替・スライダー有効化・ZIP）全パス
- 本番（GitHub Pages）でも全パス
